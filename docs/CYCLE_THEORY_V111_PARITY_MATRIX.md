# Cycle Theory V111 — MQ5 ↔ HAGMARTK Parity Matrix

Audit date: 2026-08-29
Source of truth: `C:\Users\55819\Downloads\TEORIA_DOS_CICLOS ULTIMATE 1.mq5` (`#property version "111.00"`; description identifies V111)
Python scope: `backend/strategies/cycle_theory/`
Status vocabulary: PROVEN / PARTIAL / MODELLED / OPEN.

## Gate policy

No optimization, performance claim, or production promotion is allowed until all execution-critical OPEN/MODELLED rows are either proven against MT5 evidence or explicitly bounded as research assumptions.

| Contract | MQ5 source | Python implementation | Status | Audit finding |
|---|---|---|---|---|
| OnTick ordering | 1783-1817 | `research_adapter.py::on_tick` | PROVEN | Control-flow order matches source: time gate → hard stop → active trade management → closure/capital/signal. |
| Lot normalization | 272-311 | `execution_model.py::calc_lot` | PROVEN | Fixed/auto-balance, volume step/min/max and BUY-side margin quirk preserved. |
| Margin calculation | 293 | `execution_model.py::_calc_margin_buy` + `mt5_margin_evidence.py` | PROVEN | Gate 3N captured live calculation-only MT5 evidence via `order_calc_margin(ORDER_TYPE_BUY, EURUSD, 1.0, Ask)` with no `order_send`: Ask 1.15824 -> USD 231.65 margin on 1:500 account. Python model already honors explicit broker margin and preserves the BUY-side quirk. |
| Market entry side | 1376-1410 | `execution_model.py` | PROVEN | BUY uses Ask; SELL uses Bid. |
| Limit submission | 1379/1410 | `execution_model.py` + `broker.py` | PROVEN | Submitted limit price/volume preserved by current broker model. |
| Pending fill trigger | MT5 engine | `tick_execution.py` | MODELLED | BUY_LIMIT Ask<=price; SELL_LIMIT Bid>=price. Gate 3K additionally proves that the current research harness fills a gap-through limit at the submitted limit price. This is an explicit model contract, not MT5 parity; real tick/deal evidence is still required. |
| Spread gate | strategy entry functions | `signal_engine.py` | PROVEN | Gate 3N confirms source and Python use spread in integer symbol points with the same strict `> InpMaxSpread` gate, including the Expansion phase-1 quirk that does not gate spread until phase 2. Live MT5 evidence also confirmed `SYMBOL_SPREAD=4` equals `(Ask-Bid)/Point=4` on EURUSD. Historical replay spread quality remains a separate data-quality limitation, not a strategy-logic parity gap. |
| Historical spread temporal fidelity | MT5 bar `spread` + real bid/ask ticks | `ReplayBar.spread_points` held constant across each synthetic OHLC path | MODELLED | Gate 3S uses read-only Tickmill-Live evidence over the latest 60 M1 bars/tick window available on 2026-08-28. EURUSD: bar spread 1-2 pts (median 1) versus 2,006 real ticks at median 2, p95 4, max 22. XAUUSD: bar spread 10-29 pts (median 10) versus 9,336 real ticks at median 10, p95 21, max 88. Official MT5 Python/MqlRates contracts confirm bars expose one `spread` field; the replay currently holds that one value constant through O/H/L/C synthetic ticks, so intrabar spread variation is explicitly not real-tick fidelity. |
| Stops/freeze validation | 412-441 | `position_manager.py::pode_modificar` | PROVEN | Minimum distance logic mirrors source. |
| Partial exits | 1200-1264 | `position_manager.py::manage_partials` | PROVEN | Ordering, one-time volume read and minimum-volume quirks intentionally preserved. |
| Final TP sync | 1051+ | `position_manager.py::sync_target_and_visuals` | PROVEN | Expansion target and smart buffer logic mirrored. |
| Breakeven/trailing algorithm | 1073-1199 | `position_manager.py::manage_trailing` | PROVEN | Gate 3R closes an exact source-parity defect: V111 dynamic/structural trailing calls `PositionModify(ticket, novoSL, 0)`, explicitly clearing TP, while the Python port previously preserved `pos.tp`. Tests now freeze TP=0 for both branches. Source ordering is also preserved: breakeven executes before TRAIL_OFF; ATR trailing preserves current TP; dynamic/structural advance one eligible target level per successful modification. |
| PositionModify server acceptance | MT5 trade server | MockBroker `position_modify` | PARTIAL | Gate 3R proves algorithmic arguments/order, not broker acceptance. Freeze/stops pre-check logic is PROVEN, but real server rejection/rounding/race behavior remains unobserved because no live order modification is sent. |
| Pullback cancellation | 1286-1356 | `research_adapter.py::check_pending_cancellation` | PROVEN | Ordering and magic-only pending lookup quirk preserved. |
| Trade closure reset delay | 1271-1281 | `research_adapter.py::check_trade_closure` | PROVEN | 3-second guard preserved, but replay timestamp granularity may affect equivalence. |
| Restart/persistence ordering | SaveMemory/LoadMemory + active-trade OnTick | persistence + research adapter | PROVEN | Gate 3Y proves a source quirk: `LoadMemory()` is not called in OnInit; on a fresh runtime with an active trade, V111 calls `SaveMemory()` before trailing can conditionally `LoadMemory()`, potentially overwriting prior GlobalVariables with empty runtime channel state. HAGMARTK preserves and adversarially tests the same ordering; any future repair requires a new version. |
| Intrabar price path | MT5 real ticks | OHLC `_path` + `tick_historical_replay.py` | PARTIAL | Gate 3T adds read-only live tick evidence against the deterministic Bull O-L-H-C / Bear O-H-L-C model. On 120 recent closed M1 bars, EURUSD resolved 119 bars with 99 matches and 20 mismatches (83.19% match); XAUUSD resolved 120 with 109 matches and 11 mismatches (90.83%). This directly proves the OHLC path is not execution truth. A separate `CycleTheoryTickHistoricalReplay` now consumes chronological Bid/Ask ticks and preserves observed spread/order, but server-time mapping and broker fill acceptance remain unresolved. |
| SL/TP execution | MT5 trade engine | `tick_execution.py` | MODELLED | Trigger sides are correct. Gate 3M proves the current research harness executes a gap-through protective stop exactly at the submitted SL, not at the observed gap quote. This is an explicit idealized model contract, not MT5 parity; real tick/deal evidence is required before performance claims. |
| Commission/swap | 348-350 / MT5 deal history | broker/ledger | PARTIAL | Gates 3P/3V add read-only broker evidence. In the 90-day sample, all 122 XAUUSD deals with volume had non-zero commission and every observed normalized value was -USD 3.00/lot per recorded deal; one XAUUSD deal carried non-zero swap (-USD 54/lot normalized). BTCUSD/US500 showed no non-zero commission/swap in their small samples. This strengthens broker-cost evidence but is not a V111 trade sample or guaranteed future tariff. Replay remains ZERO_COMMISSION_ZERO_SWAP until a versioned effective cost model and swap contract are justified. |
| Slippage/gaps | MT5 execution | replay harness | MODELLED | Gate 3W attempted a read-only reconstruction from linked historical XAUUSD orders/deals. Sampled market orders had `ORDER_PRICE_OPEN=0.0`; MQL5 defines `ORDER_PRICE_CURRENT` as current symbol price, not original requested price. Using it as request price would fabricate slippage. Therefore no distribution is inferred and no-slippage/exact-level-fill labels remain. Real request-vs-fill evidence is still required. |
| Deviation/filling policy | OnInit: SetDeviationInPoints(InpDeviation) + SetTypeFilling(...) | replay/mock execution | PARTIAL | Gate 3O corrects the earlier audit assumption: V111 explicitly sets InpDeviation=100 points and selects FOK first, then IOC, else RETURN from SYMBOL_FILLING_MODE. Official MQL5 docs confirm deviation is the allowed deviation in points and filling type configures the order policy. Live read-only MT5 evidence on the connected terminal reported filling_mode=2 for EURUSD/XAUUSD. The replay does not model broker acceptance, partial-fill/reject/requote behavior or deviation-bounded execution, so economic execution parity remains unproven. |
| ATR calculation | `iATR` / installed MetaQuotes `ATR.mq5` | `historical_replay.py::_atr` | PROVEN | Gate 3U inspected the standard ATR source installed with MT5. It uses TR = max(high, prev close) - min(low, prev close), first value after period+1 bars, then rolling SMA recurrence. HAGMARTK had exposed ATR too early when history was short; corrected and adversarially tested. Gate 3F progressive current-bar anti-lookahead remains intact. |
| ATR handle/availability | `CopyBuffer(handleATR,0,0,1)` | broker `atr_value` publication | PARTIAL | Formula and first-valid-bar boundary are now source-proven, but direct live CopyBuffer availability/timing from the V111 EA has not been captured. |
| Current-bar OHLC visibility | `iHigh/iLow/iClose` on forming bar | `historical_replay.py` progressive partial bar | PROVEN | Replay publishes only high/low/close observed up to each synthetic tick; adversarial Gate 3J test prevents final-bar look-ahead. |
| Timezone/server time | `TimeCurrent`, `iTime` | replay datetime normalization | PARTIAL | Gate 3Q adds read-only live terminal evidence from Tickmill-Live: MT5 Python tick/bar epochs were captured as UTC for EURUSD/XAUUSD, consistent with the generic market adapter contract. This does NOT reveal the MQL5 `TimeCurrent()` broker-server wall clock offset/DST mapping. Gate 3G therefore remains valid: replay must not relabel UTC as naive server time, and explicit UTC-to-server conversion still requires direct broker/MQL5 evidence. |
| Terminal open PnL | live position state | replay mark-to-market | RESEARCH | Explicit research metric; not a synthetic realized trade. |

| Warmup/context preload semantics | EA first tick starts a fresh cycle while terminal history already exists; indicator availability is terminal-managed | replay preloads history before first strategy tick | PARTIAL | Gate 3X reframes warmup as pre-existing terminal context for a fresh start. Default 14 bars means the first evaluated candle sees 15 bars, satisfying the source-proven ATR(14) first-valid boundary, while V111 still starts its 5-candle cycle count on that first evaluated tick. Fresh-start context is bounded separately; restart persistence ordering is source-proven in Gate 3Y. |
